/**
 * Auth.js v5 (next-auth@beta) configuration stub.
 *
 * Authentication is NOT implemented yet — this file is a placeholder
 * so that `auth`, `signIn`, and `signOut` can be imported across the app
 * without errors, ready for implementation in a future sprint.
 */
import NextAuth from 'next-auth'

export const { auth, handlers, signIn, signOut } = NextAuth({
  // Providers, adapters, and callbacks will be configured here.
  providers: [],
})
